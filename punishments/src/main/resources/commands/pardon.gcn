name = "pardon"
description = "Pardons a player"

argument("type", @punish_type).argument("player", user) {
  map_result("player") = @result_to_user
  executes = pardon()
  description = "Pardons a player"
}